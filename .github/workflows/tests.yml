name: Tests

on:
  pull_request:
  push:
    branches: [ main, thpm ]
  workflow_dispatch:

jobs:
  shell-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run tests
        run: tests/run.sh
